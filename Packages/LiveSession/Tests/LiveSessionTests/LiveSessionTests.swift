import Testing
@testable import LiveSession

struct LiveSessionTests {
    @Test func packageCompiles() {
        // Placeholder — replace when M4 (Live Activity / Dynamic Island) starts.
        #expect(Bool(true))
    }
}
